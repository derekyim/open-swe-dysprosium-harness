# QA Report

## Acceptance criteria coverage

| Criterion (from SPEC) | Test type | Test location | Result |
|---|---|---|---|
| &lt;criterion 1&gt; | unit / integration / e2e / manual | `path/to/test:line` | pass / fail / deferred |
| &lt;criterion 2&gt; | … | … | … |

## Commands run

```bash
&lt;exact command&gt;
```

```
&lt;tail of output, max ~30 lines&gt;
```

## Screenshots / visual evidence

For UI changes, paste the markdown links returned by `upload_image()`:

- &lt;link 1&gt;
- &lt;link 2&gt;

## Sign-off

- **Tested:** &lt;what was verified to pass&gt;
- **Deferred:** &lt;what was intentionally not tested, with reason&gt;
- **Known limitations:** &lt;anything that works but with caveats&gt;
